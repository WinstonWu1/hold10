# Hold10 Native

React Native + Expo demo for Hold10.

## Run on phone

Install Expo Go on your phone, then run:

```bash
npm run start
```

Scan the QR code with Expo Go.

## Checks

```bash
npx tsc --noEmit
```
